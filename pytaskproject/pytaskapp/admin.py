from django.contrib import admin
from .models import User  # Import your User model (adjust as per your actual model name)

# Register your models here.
admin.site.register(User)  # Register your User model
